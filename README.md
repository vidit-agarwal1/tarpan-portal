Tarpan Admission and Monitoring System

1) Download Django, PostgreSQL, psycopg2-binary, python-dotenv in a virtual environment
2) Clone the main branch
3) In terminal, activate the virtual environment (venv) and write the command "python manage.py runserver"
